---
name: Non-reproduction report
about: You ran a REPRODUCING.md command and got a different number — the report we most want.
title: "non-repro: <table / section>"
labels: non-repro
---

**Which table** (RESULTS.md section + row):

**Exact command** (from REPRODUCING.md, with any changes called out):

```bash

```

**Seed**:

**Your number vs published**:

**Controls** — did `no_memory` / ablated stay ≈ 0? did the ceiling look sane?

**Environment**: torch / transformers / numpy versions (`pip freeze` attached if possible), Python,
GPU/CPU, backend (ROCm / CUDA / CPU):

**Run log** (attach or paste the tail — the printed carry/ablated/chance and
memory/no_memory/ceiling lines are usually where the story is):
