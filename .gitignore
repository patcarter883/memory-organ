# Checkpoints / weights (large; not redistributed — see DISCLOSURES.md)
ckpt/
*.pt

# Run logs and scratch
intlog/
*.log
*.out

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
build/
dist/

# Local env / secrets
.env

# Local GPU run-harness (box-specific ROCm image + lease/cache paths; not portable — see issue #6)
tools/local/
